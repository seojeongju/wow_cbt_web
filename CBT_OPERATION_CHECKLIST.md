# CBT 운영 점검 체크리스트

## 1) 배포 전 필수
- `sql/cbt_migration.sql` 운영 DB 적용
- 관리자 계정으로 `/admin/cbt-exams` 접속 확인
- CBT 시험 1개 이상 활성 상태인지 확인

## 2) E2E 기능 점검
1. 관리자에서 CBT 시험 생성 (과정/문항/시간/합격점수)
2. 학생 계정으로 `/student/cbt/guide` 진입
3. `/student/cbt/exams`에서 시험 시작
4. 답안 저장(문항 이동 후 재진입) 정상 여부 확인
5. 제출 후 `/student/cbt/history` 결과 표시 확인

## 3) 예외 시나리오
- 시간 만료 후 답안 저장 시 `시험 시간이 종료되었습니다.` 반환 확인
- 시간 만료 후 제출 시 결과가 `불합격` 처리되는지 확인
- 중복 제출 시 `이미 제출된 시험입니다.` 반환 확인

## 4) 관리자 운영 점검
- 목록 필터(`전체/활성/비활성`) 동작 확인
- 비활성 시험 편집 가능 여부 확인
- 시험 복제 기능(기본 비활성 생성) 확인
- 재활성화/비활성화 동작 확인

## 5) 회귀 점검
- 기존 문제풀이(`/student/exam`) 흐름 정상 동작
- 기존 시험/성적 API(`exams`, `exam-results`) 영향 여부 점검
